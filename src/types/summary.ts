export interface Summary {
    id: string
    video_url: string
    summary: string
    created_at: string
  }
  
  export interface SocialShareData {
    title: string
    text: string
    url: string
  }